<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.auction">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Auction"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProductInfoActivity"
            android:label="Product Info"/>
        <activity android:name=".BidderActivity"
            android:label="Bidder List"/>
        <activity android:name=".AuctionerActivity"
            android:label="Add Items"/>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar"/>
        <activity android:name=".MainActivity" />

    </application>

</manifest>